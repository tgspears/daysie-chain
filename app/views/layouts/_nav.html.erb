<% if @current_user %>
	<nav class="navbar navbar-default" role="navigation">
	  <!-- Brand and toggle get grouped for better mobile display -->

	  <div class="navbar-header brand-logo">
	    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".mobile-collapse">
	    <li class="mobile-user-avi">
   			<a href="/users/<%= @current_user.id %>/groups"> <h class="user-name"> <%= @current_user.firstname %> </h> <%= cl_image_tag(@current_user.image, :width => 50, :height => 50, :crop => :fill, :gravity => :face, :class => "user_picture") %></a>
    	</li>
	    <span class="btn-md btn glyphicon glyphicon-cog"><b class="caret"></b></span>
	    </button>

	    <% if @current_user %>
	    	<a class="navbar-brand" href="/user"><%= image_tag("daysie-chain-logo.png", :class=> "logo") %> | Daysie Chain</a>
	    <% else %>
	    	<a class="navbar-brand" href="/">Daysie Chain</a>
	    <% end %>
	  </div>
	  <div class="collapse navbar-collapse navbar-ex1-collapse">
	  <!-- Collect the nav links, forms, and other content for toggling -->
	    <ul class="nav navbar-nav"><% if @current_user %>
	    <% end %>
	    </ul>
	    <ul class="nav navbar-nav navbar-right">
		      <li>
		     		<a href="/users/<%= @current_user.id %>/groups"><h class="user-name"><%= @current_user.firstname %> </h><%= cl_image_tag(@current_user.image, :width => 50, :height => 50, :crop => :fill, :gravity => :face, :class => "user_picture") %></a>
	      	</li>
	   			<li class="dropdown">
		        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="btn-lg btn glyphicon glyphicon-cog"></span><b class="caret"></b></a>
		        <ul class="dropdown-menu">
							<li><a data-toggle="modal" href='#user_edit'>Edit User Settings</a></li>
		    			<li><a href="/logout">Log out</a></li>
	        	</ul>
	      </li>
	    </ul>
	  </div><!-- /.navbar-collapse -->
	  <div class="collapse navbar-collapse navbar-ex1-collapse mobile-collapse">
	    <ul class="nav navbar-nav"><% if @current_user %>
	    <% end %>
	    </ul>
	    <ul class="nav navbar-nav navbar-right">
					<li class="pull-right"><a data-toggle="modal" href='#user_edit'>Edit User Settings</a></li>
    			<li class="pull-right"><a href="/logout">Log out</a></li>
	      </li>
	    </ul>
	  </div><!-- /.navbar-collapse -->

	</nav>
<% end %>


<div class="modal fade" id="user_edit">
	<div class="modal-dialog">
		<div class="modal-content">
			<div class="modal-header">
				<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
				<h4 class="modal-title">Edit User Settings</h4>
			</div>
			<div class="modal-body">
				<p>allows user to edit their settings - phone number, email, delete account, manage blocks, etc</p>
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
				<button type="button" class="btn btn-primary">Save changes</button>
			</div>
		</div><!-- /.modal-content -->
	</div><!-- /.modal-dialog -->
</div><!-- /.modal -->